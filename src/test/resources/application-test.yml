spring:  
  datasource:
    initialization-mode: never
  jpa:
   hibernate:
    ddl-auto: create-drop 
   properties:
    org:
      hibernate:
       flushMode: ALWAYS     
logging:
  level:
    root:   
      com:
        luisgois:
          magazine:  
            DEBUG  
jwt:
 secret: test   